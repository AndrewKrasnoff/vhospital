<h4><%= @category.name %></h4>

<table class="table">
  <thead>
    <tr>
      <th>E-mail</th>
      <th>Phone</th>
      <th>Appointments</th>
      <th>Status</th>
    </tr>  
  </thead>  
  <tbody>
    <%= render @doctors %>
  <tbody>  
</table>

<% flash.now[:info] = "There are no Doctors in #{@category.name}" if @doctors.count.zero? %>
